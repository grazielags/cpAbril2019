algoritmo "SemProcedimento"
var
   num1, num2, resultado : inteiro
inicio
      num1 <- 1
      num2 <- 2
      resultado <- num1 + num2
      Escreval(resultado)
      num1 <- 4
      num2 <- 5
      resultado <- num1 + num2
      Escreval(resultado)
fimalgoritmo

