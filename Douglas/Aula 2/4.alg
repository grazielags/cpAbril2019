algoritmo "mediaarit"
var
   X, Y, Media : real
inicio

      escreva("Qual o primeiro valor? ")
      leia(X)
      escreva("Qual o segundo valor? ")
      leia(Y)
      Media <- (X+Y) / 2
escreval("A média é : ", Media )
fimalgoritmo
